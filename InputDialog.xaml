<Window x:Class="WinMix.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Text" Height="180" Width="400" WindowStartupLocation="CenterOwner" WindowStyle="SingleBorderWindow" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>            
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">            
                        <TextBlock Text="Enter a name for the new playlist:" Margin="0,0,0,8"/>
            <TextBox x:Name="InputText" MaxLength="255" MinWidth="250" Height="30" Margin="0,0,0,8" PreviewTextInput="OnPreviewTextInput"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="_Save" Width="50" Margin="0,0,8,0" IsDefault="True" Click="OnSaveButtonClick"/>
            <Button Content="_Cancel" Width="50" IsCancel="True" Click="OnCancelButtonClick"/>
        </StackPanel>
    </Grid>
</Window>
